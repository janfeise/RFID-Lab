import { createApp } from 'vue';
import App from './App.vue';

// Import Noto Sans SC and Space Grotesk
// Note: We normally link these in index.html, but let's assume they are handled by the system or index.html
createApp(App).mount('#root');
